# kms_obsidian — LLM 위키 작업 규칙

개인용 LLM 위키. 사내 Jarvis 프로젝트의 위키(wiki-fs)와는 **완전히 분리**한다.
Jarvis 쪽 내용을 이 vault에 복제하지 않고, 이 vault의 내용도 Jarvis로 넘기지 않는다.

## 1. 세 층 구조

| 층 | 폴더 | 역할 | 누가 쓰나 |
|----|------|------|-----------|
| raw (원천자료) | `05_Meetings/`, `00_Inbox/`, `03_Resources/클리핑/` | 회의록, 메모, 웹 클리핑 **원본** | 사람 |
| wiki (컴파일 결과) | `07_LLM_Wiki/` | 개념, 결정, 절차를 재구성한 문서 | LLM |
| schema (작업 규칙) | 이 파일 | 컴파일 기준 | 사람 |

## 2. 절대 규칙

1. **raw는 덮어쓰지 않는다.** 회의록 원문은 수정·요약·삭제 금지. 오탈자도 그대로 둔다.
2. **raw와 wiki를 한 파일에 섞지 않는다.** raw는 근거, wiki는 해석이다.
3. **모든 wiki 문서에 출처와 갱신일을 남긴다.** 출처는 raw 파일 경로로 적는다.
4. **확인되지 않은 내용은 단정하지 않는다.** `## 확인 필요` 항목으로 분리한다.
5. **wiki 문서를 다른 wiki 문서의 근거로 삼지 않는다.** 근거는 항상 raw여야 한다.
6. **raw가 없으면 wiki 문서를 만들지 않는다.** 빈 스텁을 미리 깔지 않는다.

5번과 6번은 과거에 실제로 깨졌던 규칙이다. 2026-04 시점에 raw 없이 wiki 스텁
6개와 프로젝트 노트 79개를 자동 생성했고, 그 문서들의 근거가 자기 자신을 가리키는
순환 참조가 되면서 vault 전체가 5개월간 멈췄다.

## 3. 컴파일 절차

회의록 하나가 들어오면:

1. `05_Meetings/YYYY/MM/YYYY-MM-DD_주제.md`에 원문 저장 (raw, 이후 수정 금지)
2. 그 회의에서 다룬 **주제별로** `07_LLM_Wiki/`의 기존 문서를 찾는다
3. 있으면 갱신, 없으면 새로 만든다 (아래 4번 형식)
4. 기존 내용과 충돌하면 덮어쓰지 말고 `07_LLM_Wiki/_system/contradictions.md`에 기록
5. 6개월 넘게 근거가 갱신되지 않은 문서는 `_system/stale-pages.md`에 기록

## 4. wiki 문서 형식

```markdown
---
type: concept | decision | procedure
updated: YYYY-MM-DD
sources:
  - 05_Meetings/2026/09/2026-09-15_주제.md
confidence: high | medium | low
---

# 문서 제목

## 한 문장 요약

## 근거
- (raw 파일 경로) — 그 자료에서 확인된 내용

## 확인된 내용

## 확인 필요

## 다음 질문
```

`근거` 항목의 모든 줄은 raw 파일을 가리켜야 한다. "이전에 정리해둔 문서에 따르면"
같은 표현은 5번 규칙 위반이다.

## 5. 민감정보

회사 개인정보(주민번호, 급여, 인사평가), 고객사 계약 정보, 미공개 재무자료는
이 vault에 넣지 않는다. 회의록에 섞여 있으면 raw 저장 단계에서 해당 줄을 지우고
`[민감정보 제외]`로 표시한다. 이건 2번 규칙(raw 보존)의 유일한 예외다.

## 6. 폴더별 현재 상태 (2026-09-21 기준)

- `01_Projects/` — 2026-04 자동 생성 스캐폴드 79개. 활성 0개. 정리 대기
- `02_Areas/`, `03_Resources/` — MOC만 있고 내용 없음
- `07_LLM_Wiki/auto/concepts/` — raw 없이 만든 스텁 6개. 재컴파일 또는 폐기 대상
- `08_Automation/openclaw/` — 자동화 프롬프트. 현재 미가동
